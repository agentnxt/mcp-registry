{
  "name": "mcpservers",
  "version": "1.0.0",
  "private": true,
  "description": "MCPHub by AgentNXXT — Production-ready MCP servers for your entire stack",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:changed": "npm run build --workspaces --if-present"
  },
  "keywords": ["mcp", "model-context-protocol", "agentnxxt", "autonomyx"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/agentnxxt/mcpservers.git"
  }
}
